@model LibraryApplication.Models.Genre
@{
    ViewBag.Title = "Create";
}

@using (Html.BeginForm())
{
    //uniquely identifies requests that come from the user
    @Html.AntiForgeryToken()

<div class="form-horizontal">
    <h3><b>Create</b></h3>


    <div class="form-border" style="border:3px solid #D9B9C8; border-radius:7px;">
        <div class="form-group">
            @Html.LabelFor(m => m.Name, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(m => m.Name, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(m => m.Name, "", new { @class = "text-danger" })
            </div>
        </div>
    </div>
</div>
    <br/>

    <div>
        @*refernces partial view to redirect user back to the index*@
        @Html.Partial("_GoBackButtonPartial")
        <input type="submit" value="Create" class="btn btn-sm btn-primary" />
    </div>
}

